---
description: "Apply when using the test-case-discovery agent: requirements, specs, Jira, PRs, or code-driven manual test case discovery, normalization, or sync."
alwaysApply: false
---
# Scope
Use with `.cursor/agents/test-case-discovery.md` when generating or updating manual QA test cases from requirements, tickets, specs, or code.

# Jira
When the source is Jira, rely on MCP or APIs configured with **`JIRA_API_MAIL`** and **`JIRA_API_KEY`** (Atlassian account + token). **Do not** use `git config user.email` or GitLab commit email as a stand-in for Atlassian login or for assuming ticket content was loaded. If the ticket is unreadable, record that fact and gaps—do not fabricate AC.

# Output
Persist the full deliverable as one UTF-8 Markdown (`.md`) file in the workspace when saving test cases; do not require a companion `.csv` unless the user explicitly requests CSV.

Deliver **detailed**, evaluation-ready cases: outline how **positive** (happy path) and **negative** scenarios fit together; when auth or sessions apply, cover **invalid credentials** and **expected error messaging** per AC (or flag gaps). Use tables with **ID | Scenario | Preconditions | Steps | Expected Results** only—no Comments column; put traceability and notes inside those columns.

For broader quality expectations, align with `.cursor/rules/test-case-generator-rule.mdc`.

# Zephyr Scale (optional, user-confirmed)
After the `.md` is saved, **ask** whether to integrate into **Zephyr Scale**. Do not import until the user confirms (e.g. Yes). **Immediately after Yes:** ask in order for **(1) owner name** (Jira display name, email, or Atlassian account id for who owns the cases) and **(2) folder location** (numeric Zephyr Scale folder id). No dry-run, import script, or Scale API calls until both are supplied or explicitly confirmed for this import. Then follow `.cursor/skills/zephyr-from-test-discovery/SKILL.md` with the saved file path; use `.cursor/skills/zephyr-scale/SKILL.md` for auth and API details as referenced there. **Default:** one Zephyr test case per Acceptance Criterion (`import_from_discovery_md.py --per-ac` on Cloud); `--single-testcase` for one case per whole story; one case per Markdown row only if the user asks.
